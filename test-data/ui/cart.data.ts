/**
 * Add to Cart UI Test Data
 */

/** Product indices on the /products page (0-based) */
export const PRODUCT_INDICES = {
  FIRST: 0,
  SECOND: 1,
  THIRD: 2,
} as const;

/** Expected default quantity when adding a product */
export const DEFAULT_QUANTITY = "1";
